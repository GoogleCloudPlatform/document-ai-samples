{
    "name": "web-app-demo",
    "env": {
        "BACKEND_URL": {
            "description": "specify the backend url you deployed to",
            "required": true
        }
    },
    "hooks": {
        "prebuild": {
            "commands": [
                "cd Backend","gcloud deploy"
            ]
        }
    }
}